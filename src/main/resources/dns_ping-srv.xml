<!--
    TCP based stack with DNS_PING discovery for Kubernetes/OpenShift environments.
    Based on JGroups tcp.xml with TCPPING replaced by DNS_PING.
-->
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="urn:org:jgroups"
        xsi:schemaLocation="urn:org:jgroups http://www.jgroups.org/schema/jgroups.xsd">
    <TCP
            bind_addr="match-interface:eth2,match-interface:eth0,site_local,loopback"
            bind_port="7800"
            recv_buf_size="0"
            send_buf_size="0"
            buffered_input_stream_size="65536"
            buffered_output_stream_size="65536"
            sock_conn_timeout="300ms"
            tcp_nodelay="${tcp-nodelay:false}"

            bundler_type="${jgroups.bundler.type:pd}"
            bundler.max_size="${jgroups.bundler.max_size:64K}"
            bundler.capacity="${jgroups.bundler.capacity:16k}"
            bundler.use_single_sender_thread="${jgroups.bundler.use_single_sender_thread:true}"
            bundler.use_ringbuffer="${jgroups.bundler.use_ringbuffer:true}"

            diag.enabled="true"
            diag.enable_tcp="true"
            thread_pool.min_threads="${jgroups.thread_pool.min_threads:0}"
            thread_pool.max_threads="${jgroups.thread_pool.max_threads:200}"
            thread_pool.keep_alive_time="60s"
            use_virtual_threads="${vthreads:true}"/>
    <dns.DNS_PING dns_address="192.168.0.17" dns_query="${jgroups.dns.dns_query:_ping._tcp.jgroups-dns-ping.myproject.svc.cluster.local}" dns_record_type="SRV"/>
    <MERGE3  min_interval="10s"
             max_interval="30s"/>
    <FD_SOCK2/>
    <FD_ALL3 timeout="40s" interval="5s"/>
    <VERIFY_SUSPECT2 timeout="1.5s"/>
    <BARRIER/>
    <pbcast.NAKACK2 use_mcast_xmit="false"/>
    <UNICAST3/>
    <pbcast.STABLE desired_avg_gossip="50s"
                   max_bytes="4M"/>
    <pbcast.GMS print_local_addr="true" join_timeout="2s"/>
    <MFC max_credits="2M"
         min_threshold="0.4"/>
    <UFC max_credits="2M"
         min_threshold="0.4"/>
    <FRAG2 frag_size="60K"/>
    <pbcast.STATE_TRANSFER/>
</config>
